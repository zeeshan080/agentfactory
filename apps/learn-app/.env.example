# =============================================================================
# Docusaurus Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in the values for local development.
# For production, set these in your CI/CD environment (e.g., GitHub Actions secrets).

# -----------------------------------------------------------------------------
# Content Source Configuration
# -----------------------------------------------------------------------------
# Controls where Docusaurus reads content from.
#
# When DISABLED (default):
#   - Docusaurus reads from docs/ folder (Git is source of truth)
#   - Authors write content directly in docs/
#   - No R2 or server connection needed
#
# When ENABLED:
#   - Docusaurus reads from build-source/ folder (hydrated from R2)
#   - CI workflow downloads content from R2 before build
#   - Use this when content is authored outside this repo
#
R2_HYDRATE_ENABLED=false

# -----------------------------------------------------------------------------
# PanaversityFS Server Configuration (for R2 sync/hydration)
# -----------------------------------------------------------------------------
# URL of the PanaversityFS MCP server.
# Only needed when R2_HYDRATE_ENABLED=true or for R2 sync workflows.
# - Local development: http://localhost:8000/mcp
# - Production: Your hosted server URL
PANAVERSITY_SERVER_URL=http://localhost:8000/mcp

# API Key for PanaversityFS MCP server authentication.
# Get this from the Panaversity SSO server (API key management).
# Required when server has auth enabled. Leave empty for dev mode (no auth).
PANAVERSITY_API_KEY=

# -----------------------------------------------------------------------------
# AgentFactory API (Lesson Personalization)
# -----------------------------------------------------------------------------
# URL of the AgentFactory API for AI-powered lesson personalization.
# - Local development: http://localhost:8080
# - Production: https://agentfactory-api-182510399732.us-central1.run.app
AGENTFACTORY_API_URL=http://localhost:8080

# -----------------------------------------------------------------------------
# Authentication Configuration (SSO)
# -----------------------------------------------------------------------------
# URL of the authentication server for login/signup redirects
# This is the central SSO server shared across Panaversity books
AUTH_URL=http://localhost:3001

# OAuth Client ID
# Pre-configured trusted client (PKCE + JWKS)
# This should match the trustedClients configuration in auth-server
OAUTH_CLIENT_ID=agent-factory-public-client

# -----------------------------------------------------------------------------
# Google Analytics 4 (Optional)
# -----------------------------------------------------------------------------
# Your GA4 measurement ID for analytics tracking.
# Leave empty to disable analytics.
# GA4_MEASUREMENT_ID=G-XXXXXXXXXX
